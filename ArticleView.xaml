<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="REMNANT.ArticleView" Title="ArticleView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackLayout Background="#121212" Grid.Row="0">
            <Button Text="✨" WidthRequest="50" Background="#121212" HorizontalOptions="End"
                    Margin="20,0" Clicked="GetAISummary"/>
        </StackLayout>
        <WebView Grid.Row="1" x:Name="BrowserSession" HorizontalOptions="Fill" VerticalOptions="Fill"/>
    </Grid>
</ContentPage>